pipeline {
    agent any
    
    stages {
        stage('Instalacao npm'){
            steps {
                echo 'Instalando...'
                sh '''
                    
                    npm install

                '''
            }
        }
        
    }
}